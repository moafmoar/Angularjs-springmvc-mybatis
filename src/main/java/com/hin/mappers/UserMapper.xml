<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hin.dao.UserDao">

	<resultMap type="com.hin.entity.User" id="UserResult">
		<result property="userId" column="userId"/>
		<result property="userName" column="userName"/>
		<result property="password" column="password"/>
	</resultMap>
	
	<select id="login" parameterType="User" resultMap="UserResult">
		select * from t_user where userName=#{userName} and password=#{password}
	</select>
	
	<select id="getUserById" parameterType="User" resultMap="UserResult">
		select * from t_user where userId=#{id}
	</select>
	
	<select id="getAllUsers" parameterType="User" resultMap="UserResult">
		select * from t_user
	</select>
	
	<insert id="addUser" parameterType="User">
		insert into t_user (userName) values (#{userName});
	</insert>
	
	<delete id="deleteUser" parameterType="java.lang.String">
		delete from t_user where userName = #{userName}
	</delete>
	
	<delete id="deleteAll" parameterType="java.lang.String">
		delete from t_user
	</delete>

	<update id="updateUser" parameterType="java.lang.Integer">
		UPDATE t_user SET userName = 'sd',password='123456' WHERE userId = #{userId}
	</update>
	

</mapper> 